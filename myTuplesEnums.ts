const User : (string | number)[] = [1,'hc'];

//tuple form
let tUser: [string, number,boolean]

tUser = ['fd',233,true]


let rgb: [number, number, number, ] = [1,1,1,]

type  User = [string, number, ]

const newUser: User = ['bad',231] 

newUser[0] = 'good'
newUser.push(1)

















export {}